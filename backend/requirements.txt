# Core — that's all you need to run the app (SQLite is in the Python stdlib).
fastapi>=0.110
uvicorn[standard]>=0.29

# Powers real Claude output when AI_PROVIDER=anthropic + ANTHROPIC_API_KEY are set
# (otherwise unused — the app defaults to the built-in mock brain).
anthropic>=0.40
